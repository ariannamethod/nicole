# Nicole - Neural Organism Intelligence Conversational Language Engine
# Minimal dependencies for CPU-only system without heavy libraries

# Telegram bot and HTTP requests
python-telegram-bot==20.7
requests==2.31.0

# System monitoring for AMLK API
psutil>=5.9.0

# For async operations (if needed)
asyncio-mqtt==0.16.1

# For web interface (optional)
flask==3.0.0
flask-cors==4.0.0

# Everything else - Python standard library:
# - sqlite3 (built-in)
# - json (built-in)
# - time (built-in)
# - threading (built-in)
# - math (built-in)
# - random (built-in)
# - collections (built-in)
# - typing (built-in)
