#!/usr/bin/env python3
"""Быстрый тест Subjectivity с ускоренными циклами"""

import time
from nicole_subjectivity import SubjectivityCore

print("="*70)
print("🧪 БЫСТРЫЙ ТЕСТ SUBJECTIVITY (10 сек циклы)")
print("="*70)

# Создаём экземпляр
subjectivity = SubjectivityCore()

# Ускоряем циклы для теста: 10 сек вместо 1 часа
subjectivity.circadian_timer.cycle_duration = 10

# Симулируем стимул от юзера
print("\n⚡ Стимул от юзера...")
subjectivity.on_user_stimulus("Привет Nicole! Расскажи о природе сознания и резонансе.")

# Запускаем автономное сознание
print("\n🌊 Запускаем поток сознания...")
subjectivity.start_circadian_cycles()

# Ждём 3 цикла (30 сек)
print("\n⏳ Ждём 3 цикла (30 секунд)...")
for i in range(3):
    print(f"\n--- Цикл {i+1}/3 (ждём 10 сек) ---")
    time.sleep(11)  # 11 сек чтобы точно сработал

# Второй стимул в середине
print("\n⚡ Новый стимул от юзера (сброс центра волн)...")
subjectivity.on_user_stimulus("А что думаешь об искусственном интеллекте?")

# Ещё 2 цикла
print("\n⏳ Ждём ещё 2 цикла (20 секунд)...")
for i in range(2):
    print(f"\n--- Цикл {i+4}/5 (ждём 10 сек) ---")
    time.sleep(11)

# Останавливаем
print("\n🛑 Останавливаем поток сознания...")
subjectivity.stop_circadian_cycles()

# Результаты
print("\n" + "="*70)
print("📊 РЕЗУЛЬТАТЫ")
print("="*70)

# Получаем субъективный контекст
print("\n💭 Субъективный контекст для ответа юзеру:")
context = subjectivity.get_subjective_context(limit=5)
print(context)

# Показываем все мысли
print("\n🧠 Все мысли из потока сознания:")
recent_thoughts = subjectivity.get_recent_thoughts(limit=20)
for i, thought in enumerate(recent_thoughts, 1):
    print(f"\n{i}. Цикл #{thought.cycle_number} | Волна: {thought.wave_distance:.2f} | Резонанс: {thought.resonance_with_user:.2f}")
    print(f"   Эмоция: {thought.emotional_state}")
    print(f"   Мысль: {thought.thought_content[:100]}...")

print("\n" + "="*70)
print("✅ ТЕСТ ЗАВЕРШЁН")
print("="*70)
